var grunt = require('grunt');

exports.glob = {
    basic: function(test) {
        var actual = grunt.file.read('tmp/basic.html');
        var expected = grunt.file.read('test/expected/basic.html');

        test.equal(actual, expected, 'basic glob replacement');
        test.done();
    }
};
